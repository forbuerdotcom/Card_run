<UserControl x:Class="Card_run.Views.PreparationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="1080" d:DesignWidth="1920" Background="SlateGray">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*"/>
            <ColumnDefinition Width="250"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Row="0" Grid.Column="0" Background="Transparent" Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Панель ограничений -->
            <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Left" Margin="5">
                <Border ToolTip="кол-во карт в колоде" Background="Transparent" Margin="5">
                    <StackPanel Orientation="Horizontal">
                        <Image Source="pack://application:,,,/Assets/cards_collection.png" Width="30" Height="30"/>
                        <TextBlock x:Name="DeckCountText" Text="0/5" VerticalAlignment="Center" Margin="5,0,0,0" FontWeight="Bold" FontSize="16"/>
                    </StackPanel>
                </Border>
                <Border ToolTip="кол-во карт с показателем силы 9-10" Background="Transparent" Margin="5">
                    <StackPanel Orientation="Horizontal">
                        <Image Source="pack://application:,,,/Assets/card.png" Width="30" Height="30"/>
                        <TextBlock x:Name="HighPowerCountText" Text="0/1" VerticalAlignment="Center"  Margin="5,0,0,0" FontWeight="Bold" FontSize="16"/>
                    </StackPanel>
                </Border>
                <Border ToolTip="кол-во карт с показателем силы 7-8" Background="Transparent" Margin="5">
                    <StackPanel Orientation="Horizontal">
                        <Image Source="pack://application:,,,/Assets/card_outline.png" Width="30" Height="30"/>
                        <TextBlock x:Name="MidPowerCountText" Text="0/3" VerticalAlignment="Center" Margin="5,0,0,0" FontWeight="Bold" FontSize="16"/>
                    </StackPanel>
                </Border>
                <Border ToolTip="суммарный показатель силы колоды" Background="Transparent" Margin="5">
                    <StackPanel Orientation="Horizontal">
                        <Image Source="pack://application:,,,/Assets/sword.png" Width="30" Height="30"/>
                        <TextBlock x:Name="TotalPowerText" Text="0/35" VerticalAlignment="Center" Margin="5,0,0,0" FontWeight="Bold" FontSize="16"/>
                    </StackPanel>
                </Border>
            </StackPanel>

            <!-- ... (остальная часть XAML без изменений) ... -->
            <Border Grid.Row="1" BorderBrush="Gold" BorderThickness="2" CornerRadius="5" Background="#FF4A4A4A">
                <Grid>
                    <TextBlock x:Name="DeckPlaceholder" Text="Ваша колода (кликните на карты ниже, чтобы добавить)" 
                               HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="24" Foreground="LightGray"/>
                    <WrapPanel x:Name="PlayerDeckPanel" Orientation="Horizontal" Margin="10"/>
                </Grid>
            </Border>
        </Grid>

        <Border Grid.Row="0" Grid.Column="1" Grid.RowSpan="2" BorderBrush="Silver" BorderThickness="3" CornerRadius="5" Margin="10" Background="#FF333333">
            <StackPanel x:Name="CardInfoPanel" Margin="10" Visibility="Collapsed">
                <TextBlock x:Name="InfoName" FontSize="18" FontWeight="Bold" Foreground="White" TextWrapping="Wrap"/>
                <TextBlock x:Name="InfoType" FontSize="14" Foreground="Cyan"/>
                <TextBlock x:Name="InfoPower" FontSize="14" Foreground="Yellow"/>
                <Line X1="0" Y1="0" X2="220" Y2="0" Stroke="Gray" StrokeThickness="1" Margin="0,5"/>
                <TextBlock x:Name="InfoDefenceMove" FontSize="14" Foreground="Magenta"/>
                <TextBlock x:Name="InfoAttackType" FontSize="14" Foreground="RosyBrown"/>
                <TextBlock x:Name="InfoDefenceType" FontSize="14" Foreground="White"/>
                <TextBlock x:Name="InfoDefenceWeaknesses" FontSize="14" Foreground="Moccasin"/>
                <TextBlock x:Name="InfoHP" FontSize="14" Foreground="Lime"/>
                <TextBlock x:Name="InfoAD" FontSize="14" Foreground="Red"/>
                <TextBlock x:Name="InfoSpeed" FontSize="14" Foreground="LightBlue"/>
                <TextBlock x:Name="InfoDefence" FontSize="14" Foreground="LightGray"/>
                <Line X1="0" Y1="0" X2="220" Y2="0" Stroke="Gray" StrokeThickness="1" Margin="0,5"/>
                <TextBlock x:Name="InfoPerk" FontSize="14" Foreground="Orange"/>
            </StackPanel>
        </Border>

        <Border Grid.Row="1" Grid.Column="0" BorderBrush="Black" BorderThickness="2" CornerRadius="5" Margin="10" Background="#FF2A2A2A">
            <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                <WrapPanel x:Name="AllCardsPanel" Orientation="Horizontal" Margin="10"/>
            </ScrollViewer>
        </Border>

        <StackPanel Grid.Row="1" Grid.Column="1" Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="10">
            <Button x:Name="StartGameButton" Content="Начать забег" Width="150" Height="40" Margin="0,5" IsEnabled="False" Click="StartGame_Click"/>
            <Button x:Name="ClearDeckButton" Content="Очистить колоду" Width="150" Height="40" Margin="0,5" Click="ClearDeck_Click"/>
            <Button Content="Инструкция" Width="150" Height="40" Margin="0,5" Click="ShowInstruction_Click"/>
            <Button Content="В меню" Width="150" Height="40" Margin="0,5" Click="BackToMenu_Click"/>
        </StackPanel>

    </Grid>
</UserControl>