<!-- Views/BattleView.xaml -->
<UserControl x:Class="Card_run.Views.BattleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="1080" d:DesignWidth="1920">
    <Grid Background="SlateGray">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250"/>
            <!-- Очередь -->
            <ColumnDefinition Width="*"/>
            <!-- Поле боя -->
            <ColumnDefinition Width="250"/>
            <!-- Боевой журнал -->
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <!-- Поле врага -->
            <RowDefinition Height="Auto"/>
            <!-- Разделитель -->
            <RowDefinition Height="*"/>
            <!-- Поле игрока -->
            <RowDefinition Height="Auto"/>
            <!-- Рука игрока -->
        </Grid.RowDefinitions>

        <!-- Очередь ходов -->
        <Border Grid.Column="0" Grid.Row="0" Grid.RowSpan="4" BorderBrush="Gold" BorderThickness="3" CornerRadius="5" Margin="10" Background="Black">
            <StackPanel x:Name="TurnOrderPanel" Margin="10">
                <TextBlock Text="Очередь:" FontSize="20" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center"/>
            </StackPanel>
        </Border>

        <!-- Боевой журнал -->
        <Border Grid.Column="2" Grid.Row="0" Grid.RowSpan="4" BorderBrush="Silver" BorderThickness="3" CornerRadius="5" Margin="10" Background="#FF333333">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" Text="Сводка боя:" FontSize="20" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center" Margin="0,5"/>
                <!-- ScrollViewer нужен, чтобы журнал можно было прокручивать -->
                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="5">
                    <StackPanel x:Name="BattleLogPanel"/>
                </ScrollViewer>
            </Grid>
        </Border>

        <!-- Информация о карте при наведении -->
        <Border x:Name="CardInfoBorder" Grid.Column="1" Grid.Row="0" Background="Black" Opacity="0.8" 
                CornerRadius="10" Padding="15" Margin="10" HorizontalAlignment="Left" VerticalAlignment="Top"
                Visibility="Collapsed">
            <StackPanel>
                <TextBlock x:Name="InfoName" FontSize="18" FontWeight="Bold" Foreground="White"/>
                <TextBlock x:Name="InfoHP" FontSize="14" Foreground="Lime"/>
                <TextBlock x:Name="InfoDefence" FontSize="14" Foreground="LightGray"/>
                <TextBlock x:Name="InfoAD" FontSize="14" Foreground="Red"/>
                <TextBlock x:Name="InfoSpeed" FontSize="14" Foreground="Cyan"/>
                <TextBlock x:Name="InfoPerk" FontSize="14" Foreground="Yellow"/>
            </StackPanel>
        </Border>

        <!-- Поле врага -->
        <Border Grid.Column="1" Grid.Row="0" Background="#FF4A4A4A" Margin="10,10,10,5" CornerRadius="10">
            <UniformGrid x:Name="EnemyField" Columns="5" Margin="10"/>
        </Border>

        <!-- Разделитель -->
        <Rectangle Grid.Column="1" Grid.Row="1" Fill="Black" Height="5" Margin="20,0" />

        <!-- Поле игрока -->
        <Border Grid.Column="1" Grid.Row="2" Background="#FF4A7A4A" Margin="10,5,10,5" CornerRadius="10">
            <UniformGrid x:Name="PlayerField" Columns="5" Margin="10"/>
        </Border>

        <!-- Рука игрока -->
        <StackPanel x:Name="PlayerHand" Grid.Column="1" Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center" Margin="10"/>

        <!-- Кнопка выхода -->
        <Button Content="Выйти из боя" Grid.Column="1" Grid.Row="3" HorizontalAlignment="Right" VerticalAlignment="Bottom" 
                Width="150" Height="40" Margin="10" Click="ExitBattle_Click"/>
        
        <!-- Кнопка инструкции -->
        <Button Content="Инструкция" Grid.Column="1" Grid.Row="3" HorizontalAlignment="Left" VerticalAlignment="Bottom" 
                Width="150" Height="40" Margin="10" Click="ShowInstruction_Click" 
                Background="DarkBlue" Foreground="White" FontWeight="Bold" BorderThickness="2" BorderBrush="Navy"/>
    </Grid>
</UserControl>