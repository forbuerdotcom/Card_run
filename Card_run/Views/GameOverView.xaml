<UserControl x:Class="Card_run.Views.GameOverView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="1080" d:DesignWidth="1920">
    <Grid Background="Black">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <TextBlock x:Name="TitleTextBlock"
                   Grid.Row="0" 
                   Text="ПОРАЖЕНИЕ" 
                   FontSize="120" 
                   FontWeight="Bold" 
                   Foreground="Red" 
                   HorizontalAlignment="Center" 
                   VerticalAlignment="Top" 
                   Margin="0,50,0,20"/>

        <!-- Статистика -->
        <StackPanel Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center">
            <!-- Очки -->
            <TextBlock Text="Заработано очков:" FontSize="30" Foreground="Gray" HorizontalAlignment="Center"/>
            <TextBlock x:Name="ScoreTextBlock" Text="0" FontSize="80" FontWeight="Bold" Foreground="Gold" HorizontalAlignment="Center" Margin="0,0,0,40"/>

            <!-- Остальная статистика -->
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="Пройдено узлов:" FontSize="24" Foreground="LightGray" Margin="10"/>
                <TextBlock x:Name="NodesVisitedTextBlock" Grid.Row="0" Grid.Column="1" Text="0" FontSize="24" Foreground="White" Margin="10" HorizontalAlignment="Right"/>

                <TextBlock Grid.Row="1" Grid.Column="0" Text="Побеждено врагов:" FontSize="24" Foreground="LightGray" Margin="10"/>
                <TextBlock x:Name="EnemiesDefeatedTextBlock" Grid.Row="1" Grid.Column="1" Text="0" FontSize="24" Foreground="White" Margin="10" HorizontalAlignment="Right"/>

                <TextBlock Grid.Row="2" Grid.Column="0" Text="Заработано золота:" FontSize="24" Foreground="LightGray" Margin="10"/>
                <TextBlock x:Name="GoldEarnedTextBlock" Grid.Row="2" Grid.Column="1" Text="0" FontSize="24" Foreground="White" Margin="10" HorizontalAlignment="Right"/>

                <TextBlock Grid.Row="3" Grid.Column="0" Text="Самый сильный враг:" FontSize="24" Foreground="LightGray" Margin="10"/>
                <TextBlock x:Name="StrongestEnemyTextBlock" Grid.Row="3" Grid.Column="1" Text="Нет" FontSize="24" Foreground="White" Margin="10" HorizontalAlignment="Right"/>
            </Grid>
        </StackPanel>

        <!-- Кнопка возврата -->
        <Button Grid.Row="2" 
                Content="В главное меню" 
                FontSize="30" 
                FontWeight="Bold" 
                Background="DarkRed" 
                Foreground="White"
                Width="300" 
                Height="70" 
                Margin="0,30,0,50"
                Click="ReturnToMainMenu_Click"/>
    </Grid>
</UserControl>