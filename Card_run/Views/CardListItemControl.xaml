<UserControl x:Class="Card_run.Views.CardListItemControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="220" d:DesignWidth="150">
    <Border x:Name="CardBorder" BorderBrush="DarkGray" BorderThickness="2" CornerRadius="8" Background="White"
            Width="120" Height="180" Margin="7" MouseEnter="Card_MouseEnter" MouseLeave="Card_MouseLeave"
            MouseLeftButtonUp="Card_MouseLeftButtonUp" Cursor="Hand">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock x:Name="CardName" Grid.Row="0" Text="{Binding Name}" FontWeight="Bold" TextAlignment="Center" TextTrimming="CharacterEllipsis" FontSize="14" Margin="2"/>

            <Image x:Name="CardImage" Grid.Row="1" Stretch="UniformToFill" Margin="5" Source="{Binding ImagePath}"/>

            <Grid Grid.Row="2" Margin="5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>

                <!-- HP -->
                <Grid Grid.Column="0">
                    <Ellipse Width="25" Height="25" Fill="Lime" Stroke="Black" StrokeThickness="1"/>
                    <TextBlock Text="{Binding CurrentHP}" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="12" FontWeight="Bold"/>
                </Grid>

                <!-- Defence -->
                <Grid Grid.Column="1">
                    <Rectangle Width="25" Height="25" Fill="Gray" Stroke="Black" StrokeThickness="1"/>
                    <TextBlock Text="{Binding Defence}" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="12" FontWeight="Bold"/>
                </Grid>

                <!-- AD -->
                <Grid Grid.Column="2">
                    <Polygon Points="0,0,35,0,18,25" Fill="Red" Stroke="Black" StrokeThickness="1"/>
                    <TextBlock Text="{Binding AD}" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="12" FontWeight="Bold" Foreground="White"/>
                </Grid>
            </Grid>
        </Grid>
    </Border>
</UserControl>